# Claude Code lokale Einstellungen
.claude/settings.local.json

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
.terraform.lock.hcl.bak

# Umgebungsvariablen mit Tokens. Steht absichtlich weit oben: das Repo ist
# public, und ein einmal gepushtes Token ist auch nach einem Force-Push
# kompromittiert.
.env
.env.*
!.env.example

# Kubeconfig / Credentials
kubeconfig
kubeconfig.yaml
*.age
age.key

# macOS
.DS_Store
